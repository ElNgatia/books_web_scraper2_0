name: books_web_scraper2_0
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
  build_runner: ^2.4.6
  html: ^0.15.4
  http: ^1.1.0
  json_annotation: ^4.8.1
  orm: ^3.4.4
  # path: ^1.8.0

dev_dependencies:
  json_serializable: ^6.7.1
  lints: ^2.0.0
  test: ^1.21.0
